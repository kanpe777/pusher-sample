%h1 Sign in
= form_tag(signin_path) do
  = label_tag :name
  = text_field_tag :name
  = label_tag :password
  = password_field_tag :password
  = submit_tag("Sign in")
%p New user? #{link_to "Sign up now!", signup_path}
